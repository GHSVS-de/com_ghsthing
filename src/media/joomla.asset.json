{
  "$schema": "https://developer.joomla.org/schemas/json-schema/web_assets.json",
  "name": "{{name}}",
  "version": "{{version}}",
  "description": "{{description}}",
  "license": "{{licenseLong}}",
  "assets": [
    {
      "name": "{{name}}.css.backend",
      "type": "style",
      "uri": "{{name}}/backend.css",
      "version": "{{version}}"
    },
    {
      "name": "{{name}}.css.backend-edit",
      "type": "style",
      "uri": "{{name}}/backend-edit.css",
      "version": "{{version}}"
    }
  ]
}
